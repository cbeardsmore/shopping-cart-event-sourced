dependencies {
}

jar {
    manifest {
        attributes 'Main-Class': 'com.cbeardsmore.scart.App'
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}